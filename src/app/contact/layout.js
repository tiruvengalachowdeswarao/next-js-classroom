import Home from "../page";
const ContactLayout = (props) => {
  return (
    <div id="ContactLayout">
      {/* <Home></Home> */}
      {props.children}
    </div>
  );
};
export default ContactLayout;
