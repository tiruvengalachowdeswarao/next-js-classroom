import Home from "../page";
const Reviewlayout = (props) => {
  return (
    <div id="Reviewlayout">
      {/* <Home></Home> */}
      {props.children}
    </div>
  );
};
export default Reviewlayout;
